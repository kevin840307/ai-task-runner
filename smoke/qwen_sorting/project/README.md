# Qwen Sorting Smoke Project

This tiny project is used to verify that a real Qwen CLI run can create a
Python module with several sorting algorithms. The local validator checks the
generated implementation deterministically.
