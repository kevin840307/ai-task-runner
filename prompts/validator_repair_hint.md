Validator repair task: use only validator failure evidence relevant to the Current TODO, then edit the project implementation, documentation, or output generator that causes that failure. Ignore unrelated validator failures and later-TODO work. Do not only summarize a fix or change runner state.
Read the validator summary only to locate evidence relevant to the Current TODO, then inspect only the referenced full report needed for that repair. Fix the project implementation, not the validator or read-only fixtures.
$repeat_hint
