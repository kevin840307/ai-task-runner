Validator repair task: reproduce or inspect the first reported validator failure, then edit the project implementation, documentation, or output generator that causes it. Do not only summarize a fix or change runner state.
Read the validator summary first, then inspect only the referenced full report needed for the current repair task. Fix the project implementation, not the validator or read-only fixtures.
$repeat_hint
