Continue the existing planning work. Rewrite the current plan to resolve every judge issue below. For repair planning, keep the original goal authoritative and treat the existing implementation only as evidence; do not preserve a design merely because it already exists. Do not defend the old plan. Keep valid unaffected tasks where appropriate, do not repeat project-wide inspection, and do not implement or write files. Return the complete replacement task JSON only, using the task schema and task-count rule already established in this session.

$judge_feedback
