# Environment-specific configuration for STAGING
# This file overrides workflow-specific settings with environment values

environment:
  name: STAGING
  region: us-east-1

# Override gateway configuration for STAGING
gateway:
  base: http://fz1.fab29-staging.com
  domain: fab29-staging.com
  ssl: false

# Namespace configuration for STAGING
namespace:
  name: name-a
  tier: staging
