# Phase-specific configuration for FAB30
# This file provides phase-level overrides

# Phase configuration
phase:
  name: FAB30
  version: 1.0

# Additional phase-specific settings
phase-settings:
  timeout: 300
  retries: 3
